# Configuration

All variables live in rizin's config, so `e` works as usual:

```text
[0x00000000]> e Fr.timeout
5000
```

![Config values](img/scripting/config.png)

## The variables

| Variable | Default | Meaning | Zero means |
|---|---|---|---|
| `Fr.mem.max` | `0x100000` (1 MiB) | max bytes per `Frx`/`Frw` | no limit |
| `Fr.timeout` | `5000` ms | session and agent request timeout | falls back to 5000 |
| `Fr.hw.watchpoints` | `4` | max hardware watchpoint slots for `FrW` | agent default 4 |
| `Fr.java.max` | `512` | max classes per `FrC` request | unlimited |
| `Fr.dex.max` | `0` | max runtime classes for `FrX` | unlimited |
| `Fr.ac.min` | `2` | characters before class autocomplete | always triggers |
| `Fr.ac.max` | `12` | max autocomplete suggestions | falls back to 12 |

## Timeouts and cancellation

`Fr.timeout` bounds `Froj` and agent requests that registered the
break hook. `Frrj` and `Frcj` do not use that hook:

```text
[0x00000000]> e Fr.timeout=1000
```

A request that exceeds it fails with:

```json
{"ok":false,"error":{"code":"timeout","message":"the request timed out"}}
```

Ctrl-C cancels a hanging request:

```json
{"ok":false,"error":{"code":"cancelled","message":"the request was cancelled"}}
```

The timeout also bounds the remote pre-flight probe, so unreachable hosts
fail fast instead of hanging.

## Practical tuning

- A memory dump larger than 1 MiB: `e Fr.mem.max=0` (or a bigger cap)
- Many watchpoints on a CPU with more debug registers:
  `e Fr.hw.watchpoints=6`: the CPU is the actual cap
- Unbounded class listings: `e Fr.java.max=0`: careful: a full ART
  listing is tens of thousands of classes
- Deeper dex comparison: `e Fr.dex.max=0` (the default)
- Faster class completion: `e Fr.ac.min=1`

> **Note:** setting `Fr.timeout=0` does not mean "no timeout": the agent
> falls back to 5000 ms. Keep it at a sane value.

::: {.callout-tip}
### Try

Lower `Fr.timeout` to 100 and evaluate something slow, or to 1000 and
run a normal eval: the timeout applies to every target request.
:::

